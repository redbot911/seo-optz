services:
  - type: web
    name: seo-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    autoDeploy: true
